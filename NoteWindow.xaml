<Window x:Class="FolderNotifier.Views.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folder Note" Height="220" Width="340"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" WindowStartupLocation="Manual"
        Topmost="True" ShowActivated="False">

    <Grid x:Name="MainGrid" Margin="15">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="12" Opacity="0.15" ShadowDepth="4" Color="Black"/>
        </Grid.Effect>

        <Border x:Name="MainContainer" Background="#F4EFE6" CornerRadius="10" BorderBrush="#D1C7B7" BorderThickness="1" MouseLeftButtonDown="MainContainer_MouseDown">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="NoteTitleDisplay" Text="Title Here" Foreground="#A84C40" FontWeight="Bold" FontSize="16" TextTrimming="CharacterEllipsis"/>

                    <Button x:Name="CloseButton" Grid.Column="1" Content="✕" Width="20" Height="20" Background="Transparent" BorderThickness="0" Foreground="#A39B8E" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="CloseButton_MouseDown" Click="CloseButton_Click"/>
                </Grid>

                <Separator Grid.Row="1" Background="#E0D5C1" Margin="0,8,0,10"/>

                <TextBlock x:Name="NoteTextDisplay" Grid.Row="2" Foreground="#4A2C2A" FontSize="14" TextWrapping="Wrap" Text="Message content goes here..."/>
            </Grid>
        </Border>
    </Grid>
</Window>